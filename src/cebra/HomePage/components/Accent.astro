---
const Variants = {
  bold: 'font-semibold',
  solid: '',
  underline: 'relative bg-gradient-to-t from-20% to-20% to-transparent',
}

type Variant = keyof typeof Variants

const Colors = {
  none: { text: '', bg: '' },
  yellow: { text: 'text-cebra-accent-1', bg: 'from-cebra-accent-1' },
  sky: { text: 'text-cebra-accent-2', bg: 'from-cebra-accent-2' },
}

type Color = keyof typeof Colors

interface Props {
  variant: Variant
  color?: Color
}

const { color = 'none', variant } = Astro.props
const { text, bg } = Colors[color]

const classes = [variant === 'solid' ? text : bg, Variants[variant]]
---

<strong class:list={classes}><slot /></strong>

<style>
  strong {
    background-size: 100px 1.2em;
  }
</style>
